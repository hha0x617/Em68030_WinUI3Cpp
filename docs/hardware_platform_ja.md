# MC68030 エミュレータ (C++ WinUI3) - ハードウェアプラットフォームエミュレーション

日付: 2026-03-05

---

## プラットフォーム概要

エミュレータは2つのハードウェアプラットフォーム構成をサポート:

| プラットフォーム | 説明 | ボードタイプ設定 |
|-----------------|------|-----------------|
| Generic | カスタムコンソールと HDD を備えた最小 MC68030 システム | `"Generic"` |
| MVME147 | Motorola MVME147 VMEbus シングルボードコンピュータ | `"MVME147"` |

---

## CPU / オンチップ周辺回路（両プラットフォーム共通）

### MC68030 プロセッサ

| 項目 | 詳細 |
|------|------|
| ファイル | `Core/MC68030.h/.cpp` |
| チップ | Motorola MC68030 (32ビット) |
| レジスタ | D0-D7, A0-A7, PC, SR, SSP, VBR, CACR, SFC, DFC |
| 状態 | 完全実装済み |

### MC68030 オンチップ MMU

| 項目 | 詳細 |
|------|------|
| ファイル | `Core/Mmu.h/.cpp` |
| チップ | MC68030 内蔵 MMU |
| 機能 | CRP/SRP ルートポインタ、TC レジスタ、TT0/TT1 透過変換、ATC (1024エントリハッシュ)、多段ページテーブルウォーク、FC サポート |
| 状態 | 完全実装済み |

### MC68881/MC68882 FPU

| 項目 | 詳細 |
|------|------|
| ファイル | `Core/Fpu.h/.cpp`, `Core/FpuInstructionDecoder.h/.cpp` |
| チップ | MC68881/MC68882 互換 |
| 機能 | 8 FP レジスタ、FPCR/FPSR/FPIAR、全算術/三角/双曲線/対数演算、FMOVECR 定数 ROM |
| 状態 | 完全実装済み |

---

## Generic プラットフォーム

スタンドアロン MC68030 プログラム実行用の最小システム。

### メモリマップ

| アドレス範囲 | サイズ | 説明 |
|-------------|--------|------|
| 0x00000000 - (設定可能) | 最大 48 MB | メインRAM |
| 0x00FF0000 | 256 B | コンソールデバイス |
| 0x00FF1000 | 256 B | HDD デバイス |

### コンソールデバイス

| 項目 | 詳細 |
|------|------|
| ファイル | `IO/ConsoleDevice.h/.cpp` |
| チップ | カスタム（実ハードウェアではない） |
| ベースアドレス | 0x00FF0000（設定可能） |
| インターフェイス | TRAP #15 ハンドラによる 147Bug 互換モニタ I/O |
| 機能 | コールバック経由の文字/文字列入出力 |
| 状態 | 完全実装済み |

### HDD デバイス

| 項目 | 詳細 |
|------|------|
| ファイル | `IO/HddDevice.h/.cpp` |
| チップ | カスタム（実ハードウェアではない） |
| ベースアドレス | 0x00FF1000（設定可能） |
| インターフェイス | メモリマップドレジスタ I/O + DMA |
| セクタサイズ | 512 バイト |
| 状態 | 完全実装済み |

レジスタマップ:

| オフセット | レジスタ | 説明 |
|-----------|---------|------|
| 0x00 | Command | NOP=0, READ=1, WRITE=2, STATUS=3 |
| 0x04 | LBA | 32ビットセクタアドレス |
| 0x08 | Status | READY=0x01, ERROR=0x02 |
| 0x0C | DMA Address | 32ビット転送先メモリアドレス |

---

## MVME147 プラットフォーム

NetBSD/mvme68k および Linux/m68k を実行する Motorola MVME147 VMEbus シングルボードコンピュータをエミュレート。

### メモリマップ

| アドレス範囲 | サイズ | 説明 |
|-------------|--------|------|
| 0x00000000 - (設定可能) | 最大 32 MB | メインRAM |
| 0xFF800000 - 0xFFBFFFFF | 4 MB | ROM（オプション） |
| 0xFFFE0000 - 0xFFFE07FF | 2 KB | MK48T02 NVRAM/RTC |
| 0xFFFE1000 - 0xFFFE102F | 48 B | PCC（周辺チャネルコントローラ） |
| 0xFFFE1800 - 0xFFFE1803 | 4 B | LANCE イーサネットコントローラ |
| 0xFFFE2000 - 0xFFFE2007 | 8 B | 16550A UART（Linux のみ、仮想） |
| 0xFFFE3000 - 0xFFFE3003 | 4 B | Z8530 SCC（シリアルコントローラ） |
| 0xFFFE4000 - 0xFFFE4001 | 2 B | WD33C93 SCSI コントローラ |
| 0xFFFE8000 - 0xFFFE803F | 64 B | フレームバッファ制御（EMFB、仮想） |
| 0xFFFE9000 - 0xFFFE901F | 32 B | 入力デバイス（EMKM、仮想） |
| 0xFFFE0000 - 0xFFFEFFFF | 64 KB | I/O 空間キャッチオール（フォールバック） |

### 割り込みルーティング

全デバイス割り込みは PCC を経由:

```
Z8530 SCC  ──→ PCC ICR ($26) ──→ CPU IPL
LANCE      ──→ PCC ICR ($28) ──→ CPU IPL
WD33C93    ──→ PCC ICR ($2A) ──→ CPU IPL
Timer 1    ──→ PCC ICR ($18) ──→ CPU IPL
Timer 2    ──→ PCC ICR ($1A) ──→ CPU IPL
```

### PCC（周辺チャネルコントローラ）

| 項目 | 詳細 |
|------|------|
| ファイル | `IO/PccDevice.h/.cpp` |
| チップ | MVME147 PCC（カスタム ASIC） |
| ベースアドレス | 0xFFFE1000 |
| サイズ | 48 バイト (0x00-0x2F) |
| 状態 | 完全実装済み |

機能:
- **2 x 16ビットタイマー**: 160 kHz（壁時計ベース、CPU 速度非依存）
- **DMA コントローラ**レジスタ（テーブルアドレス、データアドレス、バイトカウント、データホールド）
- **12 割り込み制御レジスタ** (ICR): 全 MVME147 デバイス用
- **ハードウェアリセット**: RESET 命令 (0x4E70) 経由
- **ウォッチドッグタイマー** ($1D): 0xA5 書き込みでウォッチドッグを起動し即座にウォームリブートを実行（Linux `mvme147_reset()` が使用）

レジスタマップ:

| オフセット | レジスタ | 説明 |
|-----------|---------|------|
| $00-$03 | DMA Table Addr | DMA テーブルポインタ |
| $04-$07 | DMA Data Addr | DMA データポインタ |
| $08-$0B | DMA Byte Count | DMA 転送サイズ (24ビット) |
| $0C-$0F | DMA Data Hold | DMA データホールドレジスタ |
| $10-$11 | Timer 1 Preload | タイマー1 初期値 |
| $12-$13 | Timer 1 Count | タイマー1 現在値 |
| $14-$15 | Timer 2 Preload | タイマー2 初期値 |
| $16-$17 | Timer 2 Count | タイマー2 現在値 |
| $18 | Timer 1 ICR | タイマー1 割り込み制御 |
| $19 | Timer 1 Control | タイマー1 有効/COC/オーバーフロー |
| $1A | Timer 2 ICR | タイマー2 割り込み制御 |
| $1B | Timer 2 Control | タイマー2 有効/COC/オーバーフロー |
| $1D | Watchdog Timer | 0x0A=クリア、0xA5=起動（リセット発生） |
| $26 | SCC ICR | Z8530 割り込み制御 |
| $28 | LANCE ICR | AM7990 割り込み制御 |
| $2A | SCSI ICR | WD33C93 割り込み制御 |
| $2C | SW1 ICR | ソフトウェア割り込み 1 |
| $2D | Vector Base | 割り込みベクタベース（デフォルト 0x40） |
| $2E | SW2 ICR | ソフトウェア割り込み 2 |

ICR フォーマット: INT (bit 7) | IEN (bit 3) | IL[2:0] (bits 2-0)

### Z8530 SCC（シリアル通信コントローラ）

| 項目 | 詳細 |
|------|------|
| ファイル | `IO/Z8530Device.h/.cpp` |
| チップ | Zilog Z8530（デュアルチャネル SCC） |
| ベースアドレス | 0xFFFE3000 |
| サイズ | 4 バイト |
| 状態 | 完全実装済み |

機能:
- **デュアルチャネル**: チャネル A（コンソール）、チャネル B（補助）
- **16レジスタ制御モデル**（チャネルごとに WR0-WR15, RR0-RR15）
- **RX FIFO** 割り込みサポート付き
- **TX シミュレーション**: アイドル追跡と定期的な割り込み再アサート
- **DCD/CTS** 常時アサート（端末接続状態）

> **Linux に関する注意**: Linux カーネル 6.x 系では MVME147 用の Z8530 ベース tty ドライバが削除されています（`drivers/char/vme_scc.c` は 2.6.x に存在していましたが、3.x/4.x 系のクリーンアップ時に削除されました）。そのため、MVME147 上の Linux/m68k には Z8530 用のカーネル tty ドライバがありません。SCC は Linux の初期ブートコンソール（`earlyprintk`）では `arch/m68k/kernel/head.S` 内の直接レジスタアクセスにより使用されますが、ユーザー空間プログラム（init、シェル、getty）にはシリアルドライバに裏打ちされた適切な tty デバイスが必要です。最新カーネルには MVME147 用の上流 Z8530 tty ドライバが存在しないため、エミュレータは Linux 用の代替コンソールデバイスとして仮想 16550A UART（下記参照）を提供しています。NetBSD には影響ありません -- NetBSD は独自の Z8530 ドライバ（`zs(4)`）を持ち、すべてのコンソール I/O に SCC を使用します。

アドレスマップ:

| オフセット | レジスタ |
|-----------|---------|
| $0 | チャネル B 制御 |
| $1 | チャネル B データ |
| $2 | チャネル A 制御 |
| $3 | チャネル A データ |

### 仮想 16550A UART（Linux コンソール）

| 項目 | 詳細 |
|------|------|
| ファイル | `IO/Uart16550Device.h/.cpp` |
| チップ | 仮想 16550A UART（実際の MVME147 には存在しない） |
| ベースアドレス | 0xFFFE2000 |
| サイズ | 8 バイト |
| 有効化条件 | Target OS = `Linux` の場合のみ |
| 状態 | 完全実装済み |

このデバイスは実際の MVME147 ハードウェアには存在しません。Linux のユーザー空間に動作するコンソール（`/dev/ttyS0`）を提供するためにエミュレータが追加した仮想周辺デバイスです。

**背景**: 実際の MVME147 はシリアル通信に Z8530 SCC を使用します。Linux カーネルの VME ボード向け Z8530 tty ドライバ（`vme_scc.c`）はカーネル 3.x/4.x 時代に削除され、6.x には存在しません。tty ドライバがなければ、Linux は earlyprintk（アセンブリによる直接 SCC レジスタ書き込み）でカーネルメッセージを出力できますが、ユーザー空間用の `/dev/ttyS*` デバイスを提供できません。エミュレータは 0xFFFE2000 に 16550A 互換 UART をマッピングすることでこの問題を解決し、広くサポートされている `8250/16550` シリアルドライバ（`CONFIG_SERIAL_8250`）が `/dev/ttyS0` をシステムコンソールとして登録できるようにしています。

**必要なカーネル改変**: Linux カーネルソースに 2 つのパッチが必要です:
1. `arch/m68k/mvme147/config.c` -- UART をプラットフォームデバイスとして登録（`serial8250`、`mapbase=0xFFFE2000`）
2. `arch/m68k/kernel/early_printk.c` --（オプション）MVME147 での初期コンソール登録解除を防止（`keep_bootcon` サポート用）

完全なパッチ手順については [はじめに: Debian](getting_started_debian_ja.md) または [はじめに: Gentoo](getting_started_gentoo_ja.md) ガイドを参照してください。

機能:
- **16550A レジスタセット一式**: RBR/THR, IER, IIR/FCR, LCR, MCR, LSR, MSR, SCR
- **DLAB**（Divisor Latch Access Bit）: ボーレート分周レジスタアクセス用
- **MCR ループバックモード**（ビット 4）: 8250 ドライバの自動検出および FIFO サイズプロービング用
- **64 バイト受信 FIFO**
- **割り込み出力**（アクティブハイ）: RX データおよび TX 空き優先度付き
- **TX 常時レディ**: LSR は THRE と TEMT を常時セット（即時送信）

レジスタマップ (DLAB=0):

| オフセット | 読出し | 書込み |
|-----------|-------|-------|
| $0 | RBR（受信バッファ） | THR（送信ホールド） |
| $1 | IER（割り込み有効） | IER |
| $2 | IIR（割り込み ID） | FCR（FIFO 制御） |
| $3 | LCR（ライン制御） | LCR |
| $4 | MCR（モデム制御） | MCR |
| $5 | LSR（ラインステータス） | （無視） |
| $6 | MSR（モデムステータス） | （無視） |
| $7 | SCR（スクラッチ） | SCR |

レジスタマップ (DLAB=1、LCR ビット 7 セット):

| オフセット | レジスタ |
|-----------|---------|
| $0 | DLL（分周ラッチ下位） |
| $1 | DLM（分周ラッチ上位） |

### 仮想フレームバッファ制御（EMFB）

| 項目 | 詳細 |
|------|------|
| ファイル | `IO/FramebufferDevice.h/.cpp` |
| チップ | 仮想デバイス（実機 MVME147 には存在しない） |
| ベースアドレス | 0xFFFE8000 |
| サイズ | 64 バイト |
| マジック | `0x454D4642`（"EMFB"） |
| 有効化条件 | 設定でフレームバッファが有効な場合のみ |
| ステータス | 完全実装済み |

エミュレートされたフレームバッファの識別およびパレットレジスタを提供します。
VRAM 自体はメイン RAM 配列内に存在し（高速パス）、このデバイスを経由しません。

レジスタマップ:

| オフセット | R/W | レジスタ |
|-----------|-----|---------|
| $00-$03 | R | MAGIC (0x454D4642) |
| $04-$05 | R | WIDTH |
| $06-$07 | R | HEIGHT |
| $08 | R | BPP (8/16/32) |
| $0A-$0B | R | STRIDE（1行あたりのバイト数） |
| $0C-$0F | R | VRAM_BASE（物理アドレス） |
| $10-$13 | R | VRAM_SIZE（バイト数） |
| $14 | RW | ENABLE（1=有効、0=無効） |
| $20 | W | PAL_INDEX（パレットインデックス 0-255） |
| $21 | W | PAL_R（赤成分） |
| $22 | W | PAL_G（緑成分） |
| $23 | W | PAL_B（青、インデックス自動インクリメント） |

ゲストドライバ: `em68030fb` カーネルモジュール（[em68030-guest-linux](https://github.com/hha0x617/em68030-guest-linux) リポジトリ）。

### 仮想入力デバイス（EMKM）

| 項目 | 詳細 |
|------|------|
| ファイル | `IO/InputDevice.h/.cpp` |
| チップ | 仮想デバイス（実機 MVME147 には存在しない） |
| ベースアドレス | 0xFFFE9000 |
| サイズ | 32 バイト |
| マジック | `0x454D4B4D`（"EMKM"） |
| 有効化条件 | 設定でフレームバッファが有効な場合のみ |
| ステータス | 完全実装済み |

ホストからゲストへのイベント FIFO を介して仮想キーボード・マウス入力を提供します。
エミュレータのフレームバッファウィンドウでキーボード・ポインターイベントをキャプチャし、このデバイスにプッシュします。ゲストドライバは FIFO をポーリングして Linux input サブシステムまたは NetBSD wscons にイベントを報告します。

イベントタイプ: キー押下/解放（Linux `KEY_*` コード）、マウス移動（絶対/相対）、マウスボタン押下/解放（`BTN_LEFT`/`BTN_RIGHT`/`BTN_MIDDLE`）。

レジスタマップ:

| オフセット | R/W | レジスタ |
|-----------|-----|---------|
| $00-$03 | R | MAGIC (0x454D4B4D) |
| $04 | R | EVENT_COUNT（0-255、クランプ） |
| $05 | R | EVENT_TYPE（1=キー、2=マウス移動、3=マウスボタン） |
| $06-$07 | R | EVENT_CODE（スキャンコードまたはボタンコード） |
| $08-$09 | R | EVENT_VALUE（キー: 0/1、マウス移動: delta-X または abs-X） |
| $0A-$0B | R | EVENT_VALUE2（マウス移動: delta-Y または abs-Y） |
| $0C | W | EVENT_ACK（書き込みで先頭イベントをデキュー） |
| $10 | RW | IRQ_ENABLE（ビット 0） |
| $11 | R | IRQ_STATUS（ビット 0: イベント保留中） |
| $14-$15 | R | MOUSE_ABS_X |
| $16-$17 | R | MOUSE_ABS_Y |
| $18 | RW | MOUSE_MODE（0=相対、1=絶対） |
| $1C-$1D | R | SCREEN_WIDTH |
| $1E-$1F | R | SCREEN_HEIGHT |

ゲストドライバ: `em68030input` カーネルモジュール（[em68030-guest-linux](https://github.com/hha0x617/em68030-guest-linux) リポジトリ）。

**クリップボードペースト:** フレームバッファウィンドウでは Ctrl+Shift+V でクリップボードの
テキストをキーイベントとして送信できます。文字は Linux KEY_* コードに変換され、
Shift 修飾キーが自動的に処理されます（US キーボードレイアウト前提）。
CRLF 改行の CR 文字は自動的にスキップされます。

### MK48T02 NVRAM/RTC

| 項目 | 詳細 |
|------|------|
| ファイル | `IO/Mk48t02Device.h/.cpp` |
| チップ | Mostek MK48T02 (2 KB SRAM + RTC) |
| ベースアドレス | 0xFFFE0000 |
| サイズ | 2048 バイト |
| 状態 | 完全実装済み |

構造:
- **0x0000-0x07F7**: 汎用 SRAM (NVRAM ストレージ)
- **0x07F8-0x07FF**: クロックレジスタ (BCD フォーマット)

クロックレジスタ:

| オフセット | レジスタ | フォーマット |
|-----------|---------|-------------|
| $7F8 | Control | bit 7: Write, bit 6: Read |
| $7F9 | Seconds | BCD 0-59 |
| $7FA | Minutes | BCD 0-59 |
| $7FB | Hours | BCD 0-23 |
| $7FC | Day of Week | 1-7 |
| $7FD | Date | BCD 1-31 |
| $7FE | Month | BCD 1-12 |
| $7FF | Year | BCD 0-99（基準年 1968） |

MVME147 用プリセット値:
- $0774: オンボード RAM 終端+1 (32ビット BE)
- $0778-$077A: イーサネット MAC アドレス (08:00:3E プレフィックス)

### WD33C93 SCSI コントローラ

| 項目 | 詳細 |
|------|------|
| ファイル | `IO/Wd33c93Device.h/.cpp` |
| チップ | Western Digital WD33C93 (SCSI Level I) |
| ベースアドレス | 0xFFFE4000 |
| サイズ | 2 ポート |
| 状態 | 完全実装済み |

機能:
- **フェーズ単位プロトコル**: MSG_OUT -> CMD -> DATA_IN/OUT -> STATUS -> MSG_IN
- **32 内部レジスタ** ($00-$1F、間接アクセス)
- **PIO / DMA 転送**モード
- **SBT（シングルバイト転送）**モード
- **最大 8 SCSI ターゲット** (ID 0-7)

ポートアクセス:

| オフセット | 読出し | 書込み |
|-----------|-------|-------|
| $0 | ASR（補助ステータス） | アドレスレジスタ |
| $1 | データポート | データポート |

ASR ビット: INT (7), BSY (5), CIP (4), PE (1), DBR (0)

コマンド:

| コード | コマンド | 説明 |
|--------|---------|------|
| 0x00 | RESET | コントローラリセット |
| 0x01 | ABORT | 現在のコマンド中断 |
| 0x04 | DISCONNECT | 接続切断 |
| 0x06 | SEL_ATN | アテンション付きターゲット選択 |
| 0x08 | SEL_ATN_XFER | 選択 + 自動転送 |
| 0x20 | XFER_INFO | フェーズベースデータ転送 |

### SCSI ディスクターゲット

| 項目 | 詳細 |
|------|------|
| ファイル | `IO/ScsiDisk.h/.cpp` |
| インターフェイス | IScsiTarget |
| セクタサイズ | 512 バイト |
| 最大ターゲット数 | 8 (ID 0-7) |
| 状態 | 完全実装済み |

対応 SCSI コマンド:

| オペコード | コマンド | 説明 |
|-----------|---------|------|
| 0x00 | TEST UNIT READY | デバイス準備確認 |
| 0x03 | REQUEST SENSE | センスデータ返却 (18バイト) |
| 0x08 | READ(6) | セクタ読出し (21ビット LBA) |
| 0x0A | WRITE(6) | セクタ書込み (21ビット LBA) |
| 0x12 | INQUIRY | デバイス識別情報 (36バイト) |
| 0x15 | MODE SELECT(6) | パラメータ受入（無視） |
| 0x1A | MODE SENSE(6) | ジオメトリ返却（ページ 3, 4） |
| 0x1B | START STOP UNIT | 起動/停止（受入） |
| 0x1E | PREVENT/ALLOW REMOVAL | メディアロック（受入） |
| 0x25 | READ CAPACITY | サイズ + ブロックサイズ返却 |
| 0x28 | READ(10) | セクタ読出し (32ビット LBA) |
| 0x2A | WRITE(10) | セクタ書込み (32ビット LBA) |
| 0x35 | SYNCHRONIZE CACHE | ディスクへフラッシュ |
| 0x55 | MODE SELECT(10) | パラメータ受入（無視） |
| 0x5A | MODE SENSE(10) | ジオメトリ返却 |

### SCSI CD-ROM ターゲット

| 項目 | 詳細 |
|------|------|
| ファイル | `IO/ScsiCdrom.h/.cpp` |
| インターフェイス | IScsiTarget |
| セクタサイズ | 2048 バイト (ISO 9660) |
| 最大ターゲット数 | 1 |
| 状態 | 完全実装済み（読取り専用） |

追加 SCSI コマンド（ディスク以外）:

| オペコード | コマンド | 説明 |
|-----------|---------|------|
| 0x43 | READ TOC | 目次情報（単一データトラック、MSF/LBA） |

制限事項: 読取り専用。WRITE コマンド未サポート。

### AM7990 LANCE イーサネットコントローラ

| 項目 | 詳細 |
|------|------|
| ファイル | `IO/LanceDevice.h/.cpp` |
| チップ | AMD AM7990 LANCE |
| ベースアドレス | 0xFFFE1800 |
| サイズ | 4 バイト |
| 状態 | 完全実装済み |

ポートアクセス:

| オフセット | レジスタ | 説明 |
|-----------|---------|------|
| $0 | RDP | レジスタデータポート（CSR 読書き） |
| $2 | RAP | レジスタアドレスポート（CSR 選択） |

CSR レジスタ:

| CSR | 説明 |
|-----|------|
| CSR0 | 制御/ステータス (INIT, STRT, STOP, TDMD, RXON, TXON, INEA, INTR, IDON, TINT, RINT 等) |
| CSR1 | 初期化ブロックアドレス下位 |
| CSR2 | 初期化ブロックアドレス上位 |
| CSR3 | バスマスタ制御 |

機能:
- **初期化ブロック**: モード、MAC アドレス、マルチキャストフィルタ、RX/TX リングディスクリプタ
- **TX/RX リング処理**: OWN ビットプロトコル
- **プラガブルネットワークバックエンド**: INetworkHandler インターフェイス経由

### ネットワークバックエンド

| バックエンド | ファイル | 説明 | 設定 |
|-------------|---------|------|------|
| VirtualNetworkHandler | `IO/VirtualNetworkHandler.h/.cpp` | 内部エコーサーバー（ARP, ICMP, TCP/UDP エコー）。ホストネットワークアクセスなし。 | NetworkMode = "Virtual"（デフォルト） |
| SlirpNetworkHandler | `IO/SlirpNetworkHandler.h/.cpp` | libslirp 経由のユーザーモード NAT。ホストネットワークへのフルアクセス。 | NetworkMode = "NAT" |
| TapNetworkHandler | `IO/TapNetworkHandler.h/.cpp` | TAP-Windows ブリッジ。ゲストがホスト LAN に直接接続。 | NetworkMode = "TAP" |

### MVME147 I/O 空間キャッチオール

| 項目 | 詳細 |
|------|------|
| ファイル | `IO/Mvme147IoSpaceDevice.h/.cpp` |
| ベースアドレス | 0xFFFE0000 |
| サイズ | 64 KB |
| 目的 | マップされていない I/O アドレスへのカーネルプローブ時のバスエラー防止 |
| 動作 | 全読出しに 0x00 を返却、全書込みを無視 |
| 状態 | 完全実装済み |

---

## デバイスサマリー

| デバイス | チップ | アドレス | プラットフォーム | 状態 |
|---------|--------|---------|-----------------|------|
| CPU | MC68030 | - | 両方 | 完了 |
| MMU | MC68030 オンチップ | - | 両方 | 完了 |
| FPU | MC68881/882 | - | 両方 | 完了 |
| コンソール | カスタム | 0x00FF0000 | Generic | 完了 |
| HDD | カスタム | 0x00FF1000 | Generic | 完了 |
| PCC | MVME147 ASIC | 0xFFFE1000 | MVME147 | 完了 |
| SCC | Zilog Z8530 | 0xFFFE3000 | MVME147 | 完了 |
| UART | 仮想 16550A | 0xFFFE2000 | MVME147 (Linux) | 完了 |
| RTC/NVRAM | Mostek MK48T02 | 0xFFFE0000 | MVME147 | 完了 |
| SCSI | WD WD33C93 | 0xFFFE4000 | MVME147 | 完了 |
| SCSI ディスク | ファイルバック | WD33C93 経由 | MVME147 | 完了 |
| SCSI CD-ROM | ISO 9660 | WD33C93 経由 | MVME147 | 完了 (R/O) |
| イーサネット | AMD AM7990 | 0xFFFE1800 | MVME147 | 完了 |
| 仮想ネット | エコーサーバー | バックエンド | MVME147 | 完了 |
| NAT ネット | libslirp | バックエンド | MVME147 | 完了 |
| TAP ブリッジ | TAP-Windows | LANCE 経由 | MVME147 | 完了 |
| フレームバッファ | 仮想 EMFB | 0xFFFE8000 | MVME147 | 完了 |
| 入力 | 仮想 EMKM | 0xFFFE9000 | MVME147 | 完了 |
| I/O キャッチオール | - | 0xFFFE0000 | MVME147 | 完了 |
