# MC68030 エミュレータ (C++ WinUI3) - 命令セット実装状況

日付: 2026-03-07

## 凡例

- [x] = 実装済み
- [ ] = 未実装
- JIT = JIT コンパイル対応（レジスタ専用またはデータページキャッシュベイルアウト付き）
  - （空欄） = JIT 未対応（未着手）

---

## 実装戦略 (C++ WinUI3)

### オペコードディスパッチ

65536 エントリの関数ポインタテーブル (`OpcodeHandler[65536]`)。各エントリは
InstructionDecoder のメンバ関数ポインタ。`InitOpcodeTable()` で初期化し、
ホットオペコードは専用 fast handler で上書き登録。

### Fast ハンドラ

高頻度オペコードは Group デコーダをバイパスして直接ディスパッチ:

| ハンドラ | オペコード | 説明 |
|---------|---------|------|
| FastMOVEQ | 0x7000-0x7EFE (bit8=0) | ムーブクイック即値。bit8=1 はテーブル未登録で DecodeGroup7 へ (不正命令) |
| FastMOVE_L_Dn_Dm | MOVE.L Dn,Dm | レジスタ間ロングワード転送 |
| FastBcc_B | 0x6x01-0x6xFE (cond!=0) | 8ビットディスプレースメント付き条件分岐 |
| FastRTS | 0x4E75 | サブルーチンからの復帰 |
| FastADD_L | ADD.L Dn,Dm | レジスタ間ロングワード加算 |
| FastSUB_L | SUB.L Dn,Dm | レジスタ間ロングワード減算 |
| FastCMP_L | CMP.L Dn,Dm | レジスタ間ロングワード比較 |

BRA.B (0x6001-0x60FE) は専用ハンドラなし。DecodeGroup6 で処理。

### 遅延レジスタスナップショット

全 Group デコーダおよび FastRTS の先頭で `_cpu.EnsureRegSnapshot()` を呼出。
バスエラーからのリカバリ用レジスタスナップショットを、実際に命令実行する直前まで
遅延させることで、スナップショット不要な JIT ブロック実行時のコストを削減。

### JIT コンパイラ

事前デコード済み `JitOp` 構造体の配列 + switch ディスパッチ。各 JitOp にはオペコード
種別・ソース/デスト・即値がデコード済みで格納される。コンパイル時にデッドフラグ除去
パスを実行し、後続命令で上書きされるフラグ計算をスキップ。

- ファイル: `Core/JitCompiler.h`, `Core/JitCompiler.cpp`
- ExecuteNextFast() と ExecuteNextFastJit() は完全に別関数。統合すると関数本体の
  肥大化により JIT OFF 時に 48→36 MHz へ低下したため分離。
- エミュレーションループ開始時にメンバ関数ポインタで選択し、命令ごとの分岐を排除。
- ブロックルックアップは ExecuteNextFastJit() 内にインライン化。ヒット時のみ
  noinline の ExecuteNextJit(block) を呼出。

### 性能

約44.14 MIPS / 約269.85 MHz サイクル (JIT OFF, Avg モード実測)、約6.1 サイクル/命令
約41.90 MIPS / 約256.17 MHz サイクル (JIT ON, -5.1% オーバーヘッド)

> **注意**: これらの数値はサイクル精度の計測値ではなく、概算の推定値です。
> - **MHz** = エミュレート済みサイクル総数 / 実時間（秒） / 1,000,000。サイクル数は
>   65,536 エントリの静的ルックアップテーブル (`s_cycleTable`) と EA コスト調整から
>   算出しており、MC68030 のタイミングを近似しますがパイプライン・キャッシュ・
>   バスウェイトステートはモデル化していません。
> - **MIPS** = エミュレート済み命令総数 / 実時間（秒） / 1,000,000。
> - **Avg** 値は Run セッション開始からの累積値。瞬時値は約 500ms 間隔でサンプリング。
> - 結果はワークロード、ホスト CPU、システム負荷により変動します。

> **Linux 実行時に MHz/MIPS が NetBSD より極端に低くなる現象について**: Linux カーネル
> 実行時、表示される MHz および MIPS の値が NetBSD と比較して大幅に低くなることが
> あります。この現象はアイドル状態のシェルプロンプト待機中に最も顕著で（例: 約 1.5 MHz
> / 約 0.2 MIPS）、ブート中にも確認できます。主に以下の2つの要因によるものです。
>
> 1. **STOP 命令によるアイドル時間** — Linux は割り込み待ちに M68K の `STOP` 命令
>    (0x4E72) を使用して CPU を停止させます（割り込み駆動型アイドル）。STOP 中、
>    エミュレータは命令を実行せずサイクルも加算しませんが、実時間は経過し続けます。
>    MHz と MIPS はサイクル数（または命令数）を実時間で除算して算出するため、STOP
>    期間が長いと両指標が大きく低下します。シェルプロンプト待機中はほぼ全時間が
>    STOP 状態のため、値は極端に低くなります。一方 NetBSD はビジーウェイトループで
>    命令を連続実行する傾向があり、アイドル時でもカウンタが高い値を維持します。
>
> 2. **SCSI 割り込み処理の不完全さ** — WD33C93 SCSI コントローラのエミュレーションが、
>    Linux カーネルドライバが期待するすべての割り込みシナリオに対応していません。
>    SCSI ディスクプローブ中、未処理の割り込み条件によりドライバがポーリングループに
>    入ったり、到着しないイベントを待ち続けたりするため、実効的な命令スループットが
>    さらに低下します。これは WD33C93 の割り込みエミュレーション改善により対処予定の
>    既知の制限事項です。
>
> 低い値は MHz/MIPS 計測自体のバグを示すものではなく、エミュレートされた CPU が
> 大部分の時間をアイドルまたはストール状態で過ごしていることを正確に反映しています。

### サイクルテーブル

`s_cycleTable[65536]` (uint8_t): 静的ルックアップテーブル。EA コスト関数が
アドレッシングモードごとのメモリアクセスオーバーヘッドを計算。

### ソースファイル

| ファイル | 説明 |
|---------|------|
| Core/InstructionDecoder.h/.cpp | 整数命令デコーダ（約3200行） |
| Core/FpuInstructionDecoder.h/.cpp | FPU 命令デコーダ（約760行） |
| Core/JitCompiler.h/.cpp | JIT 基本ブロックコンパイラ |
| Core/MC68030.h/.cpp | CPU コア、オペコードテーブル、実行ループ |
| Core/Mmu.h/.cpp | MMU / アドレス変換 |

---

## 整数命令

### データ転送

| 状態 | 命令 | 説明 | JIT |
|------|------|------|-----|
| [x] | MOVE.B / .W / .L | データ転送 | JIT (.L (An)/d16(An)->Dm, (An)+->Dm, Dm->(An)/d16(An) ベイルアウト付き) |
| [x] | MOVEA.W / .L | アドレスレジスタへ転送 | JIT (.L Dn->An, An->Am) |
| [x] | MOVEQ | クイック転送（8ビット即値） | JIT |
| [x] | MOVEM | 複数レジスタ転送 | |
| [x] | MOVEP.W / .L | ペリフェラルデータ転送 | |
| [x] | MOVES | ファンクションコード付き転送 (SFC/DFC) | |
| [x] | MOVE from SR | ステータスレジスタ読出し | |
| [x] | MOVE to CCR | コンディションコードレジスタ書込み | |
| [x] | MOVE to SR | ステータスレジスタ書込み（スーパーバイザ） | |
| [x] | MOVE USP | ユーザスタックポインタ転送（スーパーバイザ） | |
| [x] | MOVEC | 制御レジスタ転送（スーパーバイザ） | |
| [x] | EXG | レジスタ交換 (Dn<->Dm, An<->Am, Dn<->An) | JIT |

### 算術演算

| 状態 | 命令 | 説明 | JIT |
|------|------|------|-----|
| [x] | ADD.B / .W / .L | 加算 | JIT (.B/.W/.L Dn,Dm) |
| [x] | ADDA.W / .L | アドレスレジスタへ加算 | |
| [x] | ADDI | 即値加算 | |
| [x] | ADDQ | クイック加算 (1-8) | JIT (.B/.W/.L Dn / An) |
| [x] | ADDX.B / .W / .L | 拡張付き加算 | |
| [x] | SUB.B / .W / .L | 減算 | JIT (.B/.W/.L Dn,Dm) |
| [x] | SUBA.W / .L | アドレスレジスタから減算 | |
| [x] | SUBI | 即値減算 | |
| [x] | SUBQ | クイック減算 (1-8) | JIT (.B/.W/.L Dn / An) |
| [x] | SUBX.B / .W / .L | 拡張付き減算 | |
| [x] | NEG.B / .W / .L | 符号反転 | JIT (.B/.W/.L Dn) |
| [x] | NEGX.B / .W / .L | 拡張付き符号反転 | |
| [x] | CLR.B / .W / .L | クリア | JIT (.B/.W/.L Dn) |
| [x] | CMP.B / .W / .L | 比較 | JIT (.B/.W/.L Dn,Dm) |
| [x] | CMPA.W / .L | アドレスレジスタと比較 | |
| [x] | CMPI | 即値比較 | |
| [x] | CMPM.B / .W / .L | メモリ間比較 (An)+,(Am)+ | |
| [x] | CMP2 / CHK2 | 範囲チェック比較 (68020+) | |
| [x] | MULU.W | 符号なし乗算 16x16->32 | JIT (Dn,Dm) |
| [x] | MULS.W | 符号付き乗算 16x16->32 | JIT (Dn,Dm) |
| [x] | MULU.L | 符号なし乗算 32x32->32/64 (68020+) | |
| [x] | MULS.L | 符号付き乗算 32x32->32/64 (68020+) | |
| [x] | DIVU.W | 符号なし除算 32/16 | |
| [x] | DIVS.W | 符号付き除算 32/16 | |
| [x] | DIVU.L | 符号なし除算 64/32 (68020+) | |
| [x] | DIVS.L | 符号付き除算 64/32 (68020+) | |

### 論理演算

| 状態 | 命令 | 説明 | JIT |
|------|------|------|-----|
| [x] | AND.B / .W / .L | 論理積 | JIT (.B/.W/.L Dn,Dm) |
| [x] | ANDI | 即値論理積 | |
| [x] | ANDI to CCR | CCR への即値論理積 | |
| [x] | ANDI to SR | SR への即値論理積（スーパーバイザ） | |
| [x] | OR.B / .W / .L | 論理和 | JIT (.B/.W/.L Dn,Dm) |
| [x] | ORI | 即値論理和 | |
| [x] | ORI to CCR | CCR への即値論理和 | |
| [x] | ORI to SR | SR への即値論理和（スーパーバイザ） | |
| [x] | EOR.B / .W / .L | 排他的論理和 | JIT (.B/.W/.L Dn,Dm) |
| [x] | EORI | 即値排他的論理和 | |
| [x] | EORI to CCR | CCR への即値排他的論理和 | |
| [x] | EORI to SR | SR への即値排他的論理和（スーパーバイザ） | |
| [x] | NOT.B / .W / .L | 論理否定 | JIT (.B/.W/.L Dn) |
| [x] | TST.B / .W / .L | テスト | JIT (.B/.W/.L Dn) |

### シフト・ローテート

| 状態 | 命令 | 説明 | JIT |
|------|------|------|-----|
| [x] | ASL.B / .W / .L | 算術左シフト (レジスタ/即値/メモリ) | JIT (.L #imm,Dn) |
| [x] | ASR.B / .W / .L | 算術右シフト (レジスタ/即値/メモリ) | JIT (.L #imm,Dn) |
| [x] | LSL.B / .W / .L | 論理左シフト (レジスタ/即値/メモリ) | JIT (.L #imm,Dn) |
| [x] | LSR.B / .W / .L | 論理右シフト (レジスタ/即値/メモリ) | JIT (.L #imm,Dn) |
| [x] | ROL.B / .W / .L | 左ローテート (レジスタ/即値/メモリ) | |
| [x] | ROR.B / .W / .L | 右ローテート (レジスタ/即値/メモリ) | |
| [x] | ROXL.B / .W / .L | 拡張付き左ローテート (レジスタ/即値/メモリ) | |
| [x] | ROXR.B / .W / .L | 拡張付き右ローテート (レジスタ/即値/メモリ) | |

### ビット操作

| 状態 | 命令 | 説明 | JIT |
|------|------|------|-----|
| [x] | BTST | ビットテスト (レジスタ/即値) | JIT (Dn,Dm) |
| [x] | BCHG | ビット変更 (レジスタ/即値) | |
| [x] | BCLR | ビットクリア (レジスタ/即値) | |
| [x] | BSET | ビットセット (レジスタ/即値) | |

### ビットフィールド (68020+)

| 状態 | 命令 | 説明 | JIT |
|------|------|------|-----|
| [x] | BFTST | ビットフィールドテスト | |
| [x] | BFCHG | ビットフィールド変更 | |
| [x] | BFCLR | ビットフィールドクリア | |
| [x] | BFSET | ビットフィールドセット | |
| [x] | BFEXTU | ビットフィールド符号なし抽出 | |
| [x] | BFEXTS | ビットフィールド符号付き抽出 | |
| [x] | BFFFO | ビットフィールド先頭ビット検索 | |
| [x] | BFINS | ビットフィールド挿入 | |

### BCD 演算

| 状態 | 命令 | 説明 | JIT |
|------|------|------|-----|
| [x] | ABCD | BCD 加算 | |
| [x] | SBCD | BCD 減算 | |
| [x] | NBCD | BCD 符号反転 | |
| [x] | PACK | BCD パック (68020+) | |
| [x] | UNPK | BCD アンパック (68020+) | |

### プログラム制御

| 状態 | 命令 | 説明 | JIT |
|------|------|------|-----|
| [x] | BRA | 無条件分岐 (.B/.W/.L) | JIT (.B/.W) |
| [x] | Bcc | 条件分岐 (.B/.W/.L)、全16条件 | JIT (.B/.W) |
| [x] | BSR | サブルーチン呼出し (.B/.W/.L) | |
| [x] | DBcc | デクリメント＆分岐、全16条件 | |
| [x] | Scc | 条件セット、全16条件 | |
| [x] | JMP | ジャンプ | |
| [x] | JSR | サブルーチンジャンプ | |
| [x] | RTS | サブルーチンからの復帰 | JIT (ベイルアウト) |
| [x] | RTR | CCR 復帰付きリターン | |
| [x] | RTE | 例外からの復帰（スーパーバイザ） | |
| [x] | RTD | ディスプレースメント付きリターン (68010+) | |
| [x] | NOP | 無操作 | JIT |
| [x] | TRAPcc | 条件トラップ (68020+) | |

### レジスタ操作

| 状態 | 命令 | 説明 | JIT |
|------|------|------|-----|
| [x] | SWAP | レジスタ上下ワード交換 | JIT |
| [x] | EXT.W | バイト→ワード符号拡張 | JIT |
| [x] | EXT.L | ワード→ロングワード符号拡張 | JIT |
| [x] | EXTB.L | バイト→ロングワード符号拡張 (68020+) | JIT |

### スタック・アドレス

| 状態 | 命令 | 説明 | JIT |
|------|------|------|-----|
| [x] | PEA | 実効アドレスプッシュ | |
| [x] | LEA | 実効アドレスロード | JIT ((An), d16(An), d8(An,Xn)) |
| [x] | LINK.W / .L | リンク＆スタックフレーム確保 | |
| [x] | UNLK | アンリンク | |

### 例外・システム

| 状態 | 命令 | 説明 | JIT |
|------|------|------|-----|
| [x] | TRAP | ソフトウェアトラップ #0-#15 | |
| [x] | TRAPV | オーバーフロー時トラップ | |
| [x] | CHK.W / .L | レジスタ範囲チェック | |
| [x] | ILLEGAL | 不正命令例外強制 | |
| [x] | RESET | 外部デバイスリセット（スーパーバイザ） | |
| [x] | STOP | CPU 停止（スーパーバイザ） | |
| [x] | TAS | テスト＆セット（アトミック） | |
| [x] | CAS | コンペア＆スワップ (68020+) | |
| [x] | CAS2 | ダブルコンペア＆スワップ (68020+) | |

---

## FPU 命令 (MC68881/MC68882)

### データ転送

| 状態 | 命令 | 説明 |
|------|------|------|
| [x] | FMOVE | FP レジスタ ⇔ メモリ/レジスタ転送 |
| [x] | FMOVECR | 定数 ROM 転送 (pi, e, ln2 等) |
| [x] | FMOVEM | 複数 FP レジスタ転送 |
| [x] | FMOVE to/from FPCR/FPSR/FPIAR | FPU 制御レジスタ転送 |

### 算術演算

| 状態 | 命令 | 説明 |
|------|------|------|
| [x] | FADD | 加算 |
| [x] | FSUB | 減算 |
| [x] | FMUL | 乗算 |
| [x] | FDIV | 除算 |
| [x] | FMOD | 剰余（IEEE、被除数の符号） |
| [x] | FREM | IEEE 剰余 |
| [x] | FABS | 絶対値 |
| [x] | FNEG | 符号反転 |
| [x] | FSQRT | 平方根 |
| [x] | FSCALE | 2のべき乗スケーリング |
| [x] | FSGLDIV | 単精度除算 |
| [x] | FSGLMUL | 単精度乗算 |
| [x] | FINT | 整数への丸め |
| [x] | FINTRZ | ゼロ方向への整数丸め |
| [x] | FGETEXP | 指数部取得 |
| [x] | FGETMAN | 仮数部取得 |

### 三角関数

| 状態 | 命令 | 説明 |
|------|------|------|
| [x] | FSIN | 正弦 |
| [x] | FCOS | 余弦 |
| [x] | FSINCOS | 正弦・余弦同時計算 |
| [x] | FTAN | 正接 |
| [x] | FASIN | 逆正弦 |
| [x] | FACOS | 逆余弦 |
| [x] | FATAN | 逆正接 |

### 双曲線関数

| 状態 | 命令 | 説明 |
|------|------|------|
| [x] | FSINH | 双曲線正弦 |
| [x] | FCOSH | 双曲線余弦 |
| [x] | FTANH | 双曲線正接 |
| [x] | FATANH | 双曲線逆正接 |

### 指数・対数関数

| 状態 | 命令 | 説明 |
|------|------|------|
| [x] | FETOX | e^x |
| [x] | FETOXM1 | e^x - 1 |
| [x] | FTWOTOX | 2^x |
| [x] | FTENTOX | 10^x |
| [x] | FLOGN | 自然対数 |
| [x] | FLOGNP1 | ln(x+1) |
| [x] | FLOG10 | 常用対数 |
| [x] | FLOG2 | 2を底とする対数 |

### 比較・分岐

| 状態 | 命令 | 説明 |
|------|------|------|
| [x] | FCMP | 比較 |
| [x] | FTST | テスト |
| [x] | FBcc | FPU 条件分岐（全32条件コード） |
| [x] | FScc | FPU 条件セット |
| [x] | FDBcc | FPU 条件デクリメント＆分岐 |
| [x] | FTRAPcc | FPU 条件トラップ |

### FPU 状態

| 状態 | 命令 | 説明 |
|------|------|------|
| [x] | FSAVE | FPU 状態保存（スーパーバイザ） |
| [x] | FRESTORE | FPU 状態復帰（スーパーバイザ） |

---

## MMU 命令 (MC68030 オンチップ)

| 状態 | 命令 | 説明 |
|------|------|------|
| [x] | PMOVE | MMU レジスタ転送 (TC, TT0, TT1, SRP, CRP, MMUSR) |
| [x] | PFLUSH | TLB エントリフラッシュ（FC とマスク指定） |
| [x] | PFLUSHA | 全 TLB エントリフラッシュ |
| [x] | PLOAD | TLB エントリプリロード (PLOADR/PLOADW) |
| [x] | PTEST | アドレス変換テスト |

---

## 未実装

| 命令 | 説明 | 備考 |
|------|------|------|
| Line-A (0xAxxx) | Line-A エミュレータトラップ | 例外10を発生（設計通り） |
| CALLM / RTM | モジュール呼出し/復帰 (68020) | 68030 で削除済み；不要 |
| cpBcc / cpDBcc / cpScc / cpTRAPcc | コプロセッサ分岐/セット (ID >= 2) | 例外11を発生 (Line-F) |
| LPSTOP | 低消費電力停止 (68060) | 68060 専用命令 |

---

## JIT まとめ

基本ブロック単位で命令を JIT コンパイル。レジスタ専用ブロックは完全に JIT 内で実行。
メモリアクセスブロックは**データページキャッシュベイルアウト**機構を使用:
キャッシュヒット時は JIT 内で継続、キャッシュミス時は当該命令の PC で
インタプリタにベイルアウト。ベイルアウト頻度が高いブロック（>64回）は
ブラックリスト化して除外。

約60種の命令パターンに対応:

| カテゴリ | 命令 |
|---------|------|
| 転送（レジスタ） | MOVEQ, MOVE.L Dn->Dm, MOVE.L An->Dn, MOVEA.L Dn->An, MOVEA.L An->Am |
| 転送（メモリ） | MOVE.L (An)->Dm, (An)+->Dm, Dm->(An), d16(An)->Dm, Dm->d16(An)（ベイルアウト） |
| 算術 | ADD/SUB/CMP .B/.W/.L (Dn,Dm), ADDQ/SUBQ .B/.W/.L (Dn, An), NEG .B/.W/.L Dn |
| 乗算 | MULU.W, MULS.W (Dn,Dm) |
| 論理 | AND/OR/EOR .B/.W/.L (Dn,Dm), NOT/CLR/TST .B/.W/.L Dn |
| シフト | ASL/ASR/LSL/LSR.L #imm,Dn |
| ビット | BTST Dn,Dm |
| レジスタ | EXG（全3形式）, SWAP Dn, EXT.W/EXT.L/EXTB.L Dn |
| アドレス | LEA (An)/d16(An)/d8(An,Xn),Ar |
| 分岐 | BRA .B/.W, Bcc .B/.W（全条件） |
| サブルーチン | RTS（ベイルアウト） |
| その他 | NOP |

### C++ JIT 実装詳細

- 事前デコード済み `JitOp` 構造体配列 + switch ディスパッチ
- デッドフラグ除去パスにより、後続命令で上書きされるフラグ計算をスキップ
- 遅延レジスタスナップショット: JIT ブロックヒット時のみ eager memcpy でスナップショット取得
- サンプリング間隔 TickInterval (256)、コンパイル閾値 = 16 ヒット

#### チューニングパラメータ

以下のパラメータは 設定 → パフォーマンス → JIT で変更可能:

- **Compile Threshold**（デフォルト: 16）: 基本ブロックがコンパイルされるまでに必要な
  サンプリング検出回数。エミュレータは TickInterval (256) 命令ごとに現在の PC を
  サンプリングし、同一ブロックアドレスがこの回数に達するとコンパイルが実行される。
  値を小さくするとより多くのブロックが早期にコンパイルされるが、コンパイルの
  オーバーヘッドも増加する。値を大きくすると最もホットなループのみがコンパイル対象になる。
  現時点では JIT は全体的にマイナスの性能影響（約 5% 低速）があるため、この値の
  調整による実用的な効果は限定的。

- **Min Block Length**（デフォルト: 3）: キャッシュ対象とする基本ブロックに必要な
  JIT コンパイル可能命令の最小数。この閾値未満のブロックはコンパイル不可として即座に
  マークされ、頻繁に実行されてもコンパイルされることはない。これにより、ブロックごとの
  セットアップコスト（スナップショット取得、ルックアップ、呼び出し）が利益を上回る
  極めて短いブロックのコンパイル・ディスパッチオーバーヘッドを回避する。値を大きくすると
  コンパイルされるブロック数が減少し、小さくするとより多くのブロックがコンパイルされるが、
  微小なブロックに対するディスパッチオーバーヘッドが増加する可能性がある。
- ExecuteNextFast()（インタプリタ）と ExecuteNextFastJit() は完全分離
  （統合すると関数本体肥大化で JIT OFF が 48→36 MHz に低下）
- ブロックルックアップは ExecuteNextFastJit() にインライン化。ヒット時のみ
  noinline の ExecuteNextJit(block) を呼出
- ベイルアウト機構: `JitExecResult { nextPC, executedCount, executedCycles }` 戻り構造体で
  部分ブロック実行に対応。メモリアクセス命令はデータページキャッシュを確認し、
  ミス時はインタプリタに復帰。64回超のベイルアウトでブラックリスト化。
- テスト: 335 件（JIT 専用テスト含む、CpuTests/JitCompilerTests.cpp）

### 性能

| モード | MHz（サイクル） | MIPS | 備考 |
|--------|----------------|------|------|
| JIT OFF | ~270 | ~44.1 | ベースライン |
| JIT ON | ~256 | ~41.9 | ベイルアウト頻度による -5.1% オーバーヘッド |
